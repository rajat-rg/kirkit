import {createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000066',
    },
    secondary:{
        main:'#0000b3'
    },
    text:{
        primary:'#ff9933', secondary:'#ffb366'
      }
  },
  
  
});

export default theme;
